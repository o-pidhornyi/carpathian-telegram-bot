package com.carpathian.bot.model;

/**
 * Supported media types for houses.  Photos and videos are the only
 * accepted formats at this time; additional types (audio, VR) could be
 * added in future revisions.
 */
public enum MediaType {
    PHOTO,
    VIDEO
}